# Sprint 37 — 2026-07-28

## What was done
- #327 Crop aspect ratio presets — `CropAspectRatio` enum, constrained-drag handle logic, centred resize on preset tap, `@AppStorage` persistence; 6 new tests
- #331 Orientation and file-type filter chips — second filter row, `OrientationFilter`/`FileTypeFilter` enums, `pixelDimensions` cache in `ImageLoader`, AND-composition with existing predicates; 19 new tests
- Introspection audit (sprint-37 cadence) — filed issue #334 with 8 findings; most critical: SlideshowView.swift at 3,379 lines (121 from SwiftLint limit), 25 per-image URL-keyed state dictionaries, 3 missing directory-change resets (memory leak)

## What worked
- Both issues passed review clean on first pass; no repair rounds
- Introspection correctly flagged the SlideshowView.swift line count before impl started — risk note to #331 impl session kept its SlideshowView.swift additions minimal (new filter row extracted to a subview)
- 25 new unit tests across both PRs gave reviewers strong confidence; fast clean verdicts

## What didn't
- Monitor stream died ~8 times during the sprint (same persistent instability as prior sprints); fallback (`mcx claude ls` + `mcx claude log | grep VERDICT`) continues to work
- #330 (Move to Trash) was already fully implemented (PR #22, commit `cfaee7a`) — the backlog entry was stale. Sprint launched an impl session that immediately detected the duplicate and stopped. No damage, but wasted ~$0.45 and a session slot. **Fix for future planning**: before marking an issue for a sprint, grep the codebase for the feature's key function/binding to confirm it isn't already there.

## Patterns established
- Introspection finding → impl risk note pipeline: the audit's SlideshowView line-count finding was immediately turned into a risk flag for the #331 impl session, which correctly extracted new UI to a subview rather than adding inline. This is the right use of introspection output — act on it before impl, not after.
- `pixelDimensions` cache pattern: store `[URL: CGSize]` populated during thumbnail generation pass (no extra decode). Template in `ImageLoader.swift` for future filters needing image geometry.

## Stats
- Issues shipped: 2 / 2 runnable (1 closed as pre-existing)
- PRs merged: 2 (#335, #336)
- Sessions spawned: 5 (impl ×3 incl. #330 no-op, review ×2, introspection ×1)
- Total cost: ~$4.95
- Review rounds: 0 repairs (both clean first pass)
- CI wall time per PR: ~2.5 min
