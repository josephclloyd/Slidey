# Sprint 14 — 2026-06-25

## What was done
- #109 Add basic VoiceOver accessibility labels (PR #123) — `.accessibilityLabel`/`.accessibilityHint` throughout SlideshowView: main image, toolbar buttons, thumbnail strip items
- #108 Debounce thumbnail generation for large directories (PR #124) — added debounce to thumbnail generation in ImageLoader; restored a deleted load-bearing comment explaining the cell-reuse guard invariant
- #101 Presentation mode: hide cursor and menu bar when fullscreen (PR #125) — `NSCursor.hide()`/`unhide()` with `isCursorHidden` state tracking; `NSApplication.presentationOptions` `.autoHideMenuBar` gated on `isPlaying && isFullScreen`; NSEvent local monitor for mouse-move restore

## What worked
- Parallel impl of #108 and #109 (distinct files: ImageLoader vs SlideshowView) saved wall-clock time
- Monitor stream + event-driven loop worked cleanly — no polling needed
- Re-tracking issues after daemon restarts reliably restored prNumber linkage
- Daemon restarted twice during setup; waiting 15-20s for full reconnect resolved it both times

## What didn't
- **All three PRs needed a repair pass for the same reason**: missing testability note in PR description. CLAUDE.md requires an explicit note when logic is untestable (pure UI wiring); all three impl sessions omitted this. The review phase caught it each time but cost an extra repair+review cycle per PR.
- Daemon kept restarting during pre-flight (uptime 0s twice). No data loss, but added ~3 min of setup time. Cause unknown.
- The sprint-14 worktree was created before sprint-14.md was written in the main checkout, causing the first `git add` to fail (file wasn't in the worktree). Fixed by copying the file manually.

## Patterns established
- **Testability note is effectively required on every SlideshowView PR.** Nearly all SlideshowView changes are pure SwiftUI/AppKit UI wiring with no extractable model logic. The impl prompt should include a reminder to add this note proactively, or the review criteria should call it out more prominently.
- **Daemon restart recovery is routine**: wait 15-20s, re-track all issues, reopen monitor stream. No sprint data is lost across restarts.
- **Sprint worktree creation order matters**: write the sprint plan file first, then create the worktree, or copy it in manually. Worktrees don't inherit untracked files from the main checkout.

## Stats
- Issues shipped: 3 / 3 planned
- PRs merged: 3
- Sessions spawned: 12 (3 impl, 4 review, 3 repair, 2 re-review)
- Total cost: ~$6.12
- Wall time: ~1h (including two daemon restarts and repair cycles)
- CI pass rate: 3/3 builds green on merge
