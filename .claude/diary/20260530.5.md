# Sprint 5 — 2026-05-30

## What was done
- #36 Music settings persistence and auto-start: `lastConfiguredMode` in UserDefaults, `resumeIfConfigured()`, Settings Music section with toggle
- #37 EXIF/image info overlay: 'i' key toggle, per-image URL-keyed state, `CGImageSource` metadata read, pill overlay matching existing style
- Issued 3 new backlog items (#37, #38, #39) from ideas proposed at plan time

## What worked
- Serializing #36 → #37 on `SlideshowView.swift` worked cleanly; no merge conflicts
- Review phase caught a real design bug in #36 (toggle scope) on the first pass; repair was focused and fast ($0.35)
- Phase resolution found PR #42 via branch lookup even when daemon polling didn't link the PR to the issue — pipeline didn't stall
- Impl sessions consistently ran build check before commit and PR; both passed first try

## What didn't
- `Closes #N` missing from both PR bodies — impl sessions didn't include it; had to be added manually after the fact
- Daemon PR-linkage polling (`prNumber` in tracked state) never resolved for #37 despite multiple untrack/re-track cycles; phase logic found the PR by branch name instead, so it was a cosmetic gap not a blocker
- Working directory drifted into the sprint-5 worktree mid-pre-flight (from the `cd` in the plan step); phantom-commit check showed a false positive; required an explicit `cd` back to main

## Patterns established
- Impl sessions need an explicit instruction to include `Closes #ISSUE` in the PR body — add to the impl phase prompt or implement.md
- Confirm worktree `cd` side-effects don't leak into pre-flight checks; always assert `git branch --show-current == main` as a standalone check before running `git log HEAD ^origin/main`

## Stats
- Issues shipped: 2 / 2 planned
- PRs merged: 2 (#41, #42)
- Sessions spawned: 5 (impl ×2, review ×3, repair ×1)
- Total cost: ~$2.73
- Wall time: ~40 min
- CI pass rate: 4/4 builds green (2 PR builds + 2 main merge builds)
