# Sprint 7 — 2026-06-04

## What was done

- #52 Updated CLAUDE.md and README: added MusicManager.swift to architecture, updated LOC count, added missing keyboard shortcuts (i, music menu)
- #53 Notifications.swift enum: extracted 23 hardcoded `NSNotification.Name("...")` literals to type-safe static constants
- #54 Home/End keys: jump to first/last image in directory
- #55 Window frame persistence: `NSWindow.setFrameAutosaveName` in AppDelegate — AppKit handles save/restore automatically
- #56 Info overlay dimensions: pixel dimensions + file size shown in the `i` overlay, loaded off main thread via `Task.detached`

## What worked

- Sprint-7 introspection cadence surfaced two real tech-debt items (#46 notification strings, #47 stale docs) that directly improved future sprint hygiene
- Parallel execution of #46 + #47, then #48 + #49, compressed wall time significantly
- `--no-execute` + `--from` override was the correct workaround for manually-spawned review sessions that skipped the phase transition
- Reviewer sessions consistently gave thorough VERDICT outputs with specific rationale

## What didn't

- Sessions used the main worktree despite `--worktree` flag — branched and left HEAD on feature branches after completing, requiring `git checkout main` recovery after every impl session
- `Closes #N` missing from 3 of 5 PR bodies (#53, #55 were caught; routine check still needed every session)
- `mcx phase run` blocked with "phases only run from branch main" when main worktree was on a feature branch — had to manually spawn review sessions and use `--from` override for done phase
- `mcx work-item transition` and `mcx work-item state set` commands failed with argument parsing errors — direct `mcx phase run --no-execute` was the workaround

## Patterns established

- After every impl session: check `Closes #N`, `git checkout main`, re-pair with untrack/track cycle — in that order, every time
- When `mcx phase run review` is blocked by branch drift: spawn reviewer manually with `mcx claude spawn --model sonnet`, then use `--no-execute` + `--from review` to advance the work item
- Sprint 7 = introspection sprint. SlideshowView.swift at 1,911 lines post-sprint — next introspection at sprint 17

## Stats

- Issues shipped: 5 / 5 planned
- PRs merged: 5 (#52–#56)
- Sessions spawned: 10 (5 impl opus + 5 review sonnet)
- Total cost: ~$5.50
- Wall time: ~1h
- CI pass rate: 5/5 builds green
