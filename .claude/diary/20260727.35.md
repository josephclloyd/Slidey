# Sprint 35 — 2026-07-27

## What was done
- #313 Saved filter presets — FilterPreset Codable model, JSON array in @AppStorage, predicate rebuild into urlFilter, popover UI on search bar; 16 unit tests (encode/decode + predicate reconstruction)
- #312 Grid / contact sheet view (Shift-T) — full-window LazyVGrid using existing thumbnail cache, keyboard arrow nav + Return to confirm, click to navigate, filter-aware; 2 unit tests

## What worked
- Both issues passed review clean on first pass — no repair rounds needed
- Risk flags were effective again: pbxproj registration correct first time on both; line limit respected; @State var (not private) used correctly; thumbnails from cache (not full decode) confirmed by reviewer
- #313's 16 tests gave the reviewer strong confidence; clean verdict was fast (didn't need to read much code beyond the test assertions)

## What didn't
- Monitor stream continued dying every few minutes (same pattern as Sprint 34) — restarted ~6 times. The issue is persistent but manageable; the idle-session check fallback (`mcx claude ls` + `mcx claude log`) works reliably when the monitor misses an event.

## Patterns established
- Grid view pattern: full-window overlay with `LazyVGrid`, `@FocusState` for keyboard nav, thumbnail from existing NSCache. Template in `SlideshowView+Grid.swift`.
- Filter preset pattern: `FilterPreset: Codable` → `@AppStorage` JSON array → predicate rebuild → `imageLoader.urlFilter`. Template in `SlideshowView+FilterPresets.swift` + `FilterPresetTests.swift`.

## Stats
- Issues shipped: 2 / 2 planned
- PRs merged: 2 (#319, #320)
- Sessions spawned: 4 (impl ×2, review ×2)
- Total cost: ~$7.40 (impl: $2.66 + $3.87, review: $0.38 + $0.54)
- Review rounds: 0 repairs (both clean first pass)
- CI wall time per PR: ~2.5 min
