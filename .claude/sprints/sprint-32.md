# Sprint 32 — 2026-07-20

Started: 2026-07-20T12:52:13Z
Status: done

## Issues

| # | Title | Files touched | Model | Blocked by |
|---|-------|--------------|-------|------------|
| 285 | Add EXIF/IPTC metadata editing | SlideshowView+Metadata.swift (new), SlideshowView.swift (HUD wiring only) | opus | — |
| 286 | Add duplicate / near-duplicate detection | DuplicateDetector.swift (new), ImageLoader.swift (scan hook), SlideshowView.swift (review overlay) | opus | #285 |
| 287 | Add side-by-side comparison view | SlideshowView.swift (heavy — split layout, new pinned-image state) | opus | #286 |

## Excluded

| # | Title | Reason |
|---|-------|--------|
| 288 | Search/filter by filename and date taken | Dropped this sprint — will pick up in sprint 33 (urlFilter baseline already exists so it's easy to slot in) |
| 289 | Export slideshow as video | Brand-new AVFoundation pipeline (AVAssetWriter); no baseline in codebase; save for later |

## Notes

- All three selected issues share SlideshowView.swift → serialized: #285 → #286 → #287. #287 is last because it makes the heaviest layout changes and rebases cleanest on top of the others.
- **#285 baseline**: `CGImageDestination` is already used in `SlideshowView+Export.swift:69`; EXIF properties are read in `ImageLoader.swift:153`. Writing metadata back uses `CGImageDestinationAddImage` with a merged metadata dict, not clobbering existing fields. Minimum scope: IPTC caption/keywords + EXIF copyright.
- **#286 scope**: Perceptual hash (average-hash or difference-hash over a downscaled grayscale thumbnail — thumbnails already generated by `ImageLoader`) is cleanly testable in `DuplicateDetector`. Review UI: a "Duplicates" filter mode in the existing thumbnail strip grouping by hash similarity; simpler than a modal. If the impl session's chosen approach doesn't fit cleanly, call it out in the PR.
- **#287 design (confirmed by Joe)**: Enter compare mode with `⌥B` (natural extension of `b`=before/after); second image is the next image in the current directory (no picker); layout is a static horizontal split with independent zoom/pan per pane; exit with Escape or `⌥B` again. First version: no zoom sync. The issue itself suggests this smaller-first approach explicitly.

## Results

Released: v1.31 — 2026-07-21

### Shipped
- #285 Add EXIF/IPTC metadata editing — caption, keywords, copyright via `I` key / File > Edit Metadata… sheet; writes back to file preserving all other metadata fields
- #286 Add duplicate / near-duplicate detection — perceptual dHash on thumbnails, Hamming distance grouping, "Duplicates" filter mode in thumbnail strip; generation-counter fix for race condition found in review
- #287 Add side-by-side comparison view — `⌥B` entry, next image as second pane, static 50/50 horizontal split, independent zoom/pan, Escape/`⌥B` to exit; extracted to `SlideshowView+Compare.swift`

### Needs attention
(none — all 3 planned issues shipped)

### Stats
- PRs merged: 3 (#300, #301, #302)
- Total cost: ~$9 (impl×3 + review×4 + repair×1)
- Sessions: ~10 (3 impl, 4 review, 1 repair, 1 resume after quota hit)
- CI wall time per PR: ~2m
- One repair round: #286 race condition (generation counter)
