# Sprint 32 — 2026-07-20

Started: 2026-07-20T12:52:13Z
Status: planned

## Issues

| # | Title | Files touched | Model | Blocked by |
|---|-------|--------------|-------|------------|
| 285 | Add EXIF/IPTC metadata editing | SlideshowView+Metadata.swift (new), SlideshowView.swift (HUD wiring only) | opus | — |
| 288 | Add search/filter by filename and date taken | ImageLoader.swift (urlFilter predicate), SlideshowView.swift (filter bar HUD) | opus | #285 |
| 286 | Add duplicate / near-duplicate detection | DuplicateDetector.swift (new), ImageLoader.swift (scan hook), SlideshowView.swift (review overlay) | opus | #288 |

## Excluded

| # | Title | Reason |
|---|-------|--------|
| 287 | Side-by-side comparison view | Heavy SlideshowView.swift layout change; interaction design (pick second image, sync zoom) not finalized |
| 289 | Export slideshow as video | Brand-new AVFoundation pipeline (AVAssetWriter); no baseline in codebase; save for sprint 33 when backlog is less crowded |

## Notes

- All three selected issues share SlideshowView.swift → serialized: #285 → #288 → #286.
- #285 and #288 also share ImageLoader.swift (EXIF read for date filtering) → further justifies the serial order.
- **#285 baseline**: `CGImageDestination` is already used in `SlideshowView+Export.swift:69`; EXIF properties are read in `ImageLoader.swift:153`. Writing metadata back uses the same `CGImageDestinationAddImage` with a metadata dict, merging rather than clobbering existing fields. Model: IPTC captions/keywords + EXIF copyright, at minimum.
- **#288 baseline**: `ImageLoader.urlFilter: ((URL) -> Bool)?` already exists and is wired into the scan pipeline (lines 90, 440, 460). The impl only needs to build a predicate from filename/date inputs and set it; the filter/unfilter machinery is already there. Key binding: check CLAUDE.md registry before picking — `/` is shortcuts overlay, `f` is scale-to-fill, `v` is favourites-only.
- **#286 scope**: Perceptual hash (average-hash or difference-hash over a downscaled grayscale thumbnail — thumbnails are already generated by `ImageLoader`) is cleanly testable. Review UI: keep it minimal — a "Duplicates" filter mode in the existing thumbnail strip, grouping by hash similarity, is simpler than a modal. If the impl session's chosen approach doesn't fit cleanly, it should call it out in the PR rather than over-engineer.
- Sprint 31 incident: #288 and #286 both touch `handleCharacterKeyPress` for new key bindings — those must not conflict. The impl session for #288 should document its chosen key; the #286 session must check that binding before picking its own.
